# modle 진행 상황

> 사내 지식 AI 어시스턴트 — 직원이 자연어로 물어보면 사내 문서 안에서 근거(출처)와 함께 답하는 권한 인식형 챗봇.
> 전체 계획은 [`계획서.md`](./계획서.md)가 authoritative. 이 파일은 **현재 스냅샷**이며 커밋마다 덮어쓴다.

최종 갱신: 2026-05-28

---

## 한눈에 보기

| Phase | 내용 | 상태 |
|---|---|---|
| **Phase 1** | 디자인 프로토타입 (mock 데이터로 모든 화면) | ✅ 완료 |
| Phase 2 | 지식 레이어 (검색/RAG, pgvector, 권한 필터) | ⬜ 미착수 |
| Phase 3 | 실제 LLM 연동 (axhub LLM, 인용·신뢰도, 스트리밍) | ⬜ 미착수 |
| Phase 4 | 제품화 (next-auth, 영속화, 관리 UI, axhub 배포) | ⬜ 미착수 |

**지금 위치: Phase 1 완료 → Phase 2 착수 직전.**

---

## 기술 스택 (현재)

- Next.js 16 (App Router) · React 19 · TypeScript
- Tailwind 3 (`preflight: false` — 이식한 assistant 디자인 CSS 보존)
- `npx next build` 클린 통과 확인됨
- 백엔드/DB/auth 아직 없음 (Phase 1은 프론트 + mock 한정)

---

## Phase 1에서 구현된 것

### 화면 (`app/`)
- `page.tsx` — 홈 (인사 히어로 + AI 브리핑 + 추천 프롬프트 칩 + 최근 대화)
- `ask/page.tsx` — 물어보기 **3-pane** (좌: 대화 히스토리 · 중앙: 채팅+컴포저 · 우: 출처/권한/신뢰도 패널)
- `sources/page.tsx` — 지식 소스 목록 (카테고리 필터 `?cat=`)
- `layout.tsx`, `globals.css` — 셸 레이아웃 + 디자인 토큰

### 컴포넌트 (`app/_components/`)
- `Shell.tsx` — 전체 레이아웃 셸
- `Sidebar.tsx` — 브랜드 + 그룹 네비 + 조직 푸터
- `Header.tsx` — AI 검색바 + 상태 + 아바타
- `CmdPalette.tsx` — ⌘K 커맨드 팔레트 (어디서나 물어보기 진입)
- `Assistant.tsx` — 어시스턴트 채팅 영역
- `icons.tsx` — 아이콘 세트

### Mock 데이터 (`app/_data/mock.ts`)
- 네비(NAV), 추천 프롬프트, 최근 대화, 브리핑 문구
- 스레드(오늘/이번주/이전), 출처 문서(SOURCES), 권한 범위(PERMS), 후속 질문(FOLLOWUPS)

---

## 다음 할 일 (Phase 2 착수)

1. 지식 소스 정의 + 수집 파이프라인 (문서 → 청크 → 임베딩 → pgvector)
2. 1차 코퍼스: 휴가/HR/근무 규정 1~2개로 작게
3. 권한 필터 검색 (부서/역할) + 관련도 점수
4. **검증 목표**: "출산휴가" 질문 시 올바른 출처와 함께 top-k 청크 반환

### 열린 결정 (Phase 2 전 확정 필요)
- 1차 코퍼스 실제 출처 (위키/노션/PDF/Push 공지 중)
- 임베딩 모델 선택
- 문서 변경 시 재색인 주기 (수동 vs 스케줄)

---

## 핵심 가드레일 (항상 유지)

- **환각 금지** — 검색 근거 안에서만 답, 근거 없으면 "모른다"
- **권한 우선 검색** — 생성 전 검색 단계에서 범위 밖 소스 제외
- **API 키 server-side 전용** — 클라이언트/로그 노출 금지
- **사내 데이터 외부망 미경유** — axhub 사내 endpoint 우선

---

## 메모

- 아직 **git 저장소 아님** (`git init` 필요) — 이 파일은 첫 커밋 이후부터 의미.
- 두 레퍼런스: `/Users/kokooa/Work/assistant`(디자인 계승), `/Users/kokooa/Work/Push`(권한/조직 모델·인프라 재활용).
