# modle 진행 상황

> 사내 지식 AI 어시스턴트 — 직원이 자연어로 물어보면 사내 문서 안에서 근거(출처)와 함께 답하는 권한 인식형 챗봇.
> 전체 계획은 [`계획서.md`](./계획서.md)가 authoritative. 이 파일은 **현재 스냅샷**이며 커밋마다 덮어쓴다.

최종 갱신: 2026-05-28

---

## 한눈에 보기

| Phase | 내용 | 상태 |
|---|---|---|
| **Phase 1** | 디자인 프로토타입 (mock 데이터로 모든 화면) | ✅ 완료 |
| **Phase 2** | 지식 레이어 (검색/RAG, 권한 필터) | ✅ 완료 (로컬 어휘검색, 임베딩/pgvector는 후속) |
| **Phase 3** | 실제 LLM 연동 (스트리밍·인용↔출처 매핑·신뢰도 산출·정직한 실패) | ✅ 완료 (Ollama 로컬) |
| **Phase 4** | 제품화 (인증·영속화·관리 UI·배포) | 🔧 진행 중 — 인증 골격 완료, 권한 인식 검색 실동작 작업 중 |

**지금 위치: Phase 4 진행 중.** 인증(Google OAuth / axhub 규약) 골격 완료. 임시 `AUTH_BYPASS` 우회 제거 완료, 다음은 부서 scope 기반 권한 검색 실동작 + axhub 등록·배포.

---

## 기술 스택 (현재)

- Next.js 16 (App Router) · React 19 · TypeScript · Tailwind 3 (`preflight: false`)
- **LLM: 로컬 Ollama `qwen2.5:7b`** (무료·비공개, server-side 전용). 앱 실행 시 `ollama serve` 필요(영속화: `brew services start ollama`)
- **DB: Postgres(로컬 `modle` DB) + Prisma 6** — Department/User/Role (Push 조직 모델 재사용)
- **인증: next-auth v5(Auth.js) + Google** — axhub 규약(`GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` env, 도메인 `jocodingax.ai` 제한)
- `npx tsc --noEmit` 클린 통과

> ⚠️ **axhub에는 LLM 추론 게이트웨이가 없음** (카탈로그 = 데이터 API + 비용 계산기뿐). 계획서의 "axhub LLM 게이트웨이"는 사실과 다름 → 직접 제공자(Ollama) 사용. `lib/llm.ts`의 `OLLAMA_BASE_URL`/`LLM_MODEL` env로 스왑 가능.
> ✅ **axhub는 로그인은 처리함** — 앱 `auth_mode`(required면 회사 로그인 게이트) + 배포 시 회사 Google 자격증명을 env 주입. modle은 Push와 동일 규약이라 배포하면 키 없이 로그인 동작. **로컬은 회사 Google 클라이언트에 localhost redirect 등록 필요**.

---

## 구현된 것

### 화면 (`app/`)
- `page.tsx` 홈 · `ask/page.tsx` 물어보기(3-pane) · `sources/page.tsx` 지식 소스
- `layout.tsx`, `globals.css`

### 컴포넌트 (`app/_components/`)
- `Shell` · `Sidebar` · `Header` · `CmdPalette`(⌘K) · `Assistant`(3-pane, 스트리밍) · `icons`

### 지식 레이어 / API (Phase 2~3)
- `content/` — 실제 코퍼스 3건(휴가·근무 규정 GLOBAL, 재무 내부 DEPARTMENT) + 권한 메타
- `lib/rag.ts` — 코퍼스 로드·청킹·**권한 필터**·문자 bigram 어휘 검색
- `lib/llm.ts` — Ollama 스트리밍 + 근거강제/인용/모르면-모른다 프롬프트
- `app/api/search/route.ts` — 권한 필터 top-k 검색(JSON)
- `app/api/ask/route.ts` — 검색→근거주입→답변 NDJSON 스트림(출처 먼저, 토큰)
- `Assistant.tsx` — 답변 `/api/ask` 스트림, 우측 출처=실시간 검색, **인용 [n] 클릭→출처 스크롤·하이라이트**(`CiteContext`), **신뢰도=검색 점수 산출**

### 인증 / 권한 (Phase 4)
- `prisma/schema.prisma` + `prisma/seed.mjs` — Department/User/Role, 시드 3명(kokooa=People Ops/MEMBER, admin=Eng/ADMIN, finance=Finance/MEMBER)
- `auth.ts` — Auth.js v5 Google(axhub 규약) + 도메인 제한 + jwt/session에 부서·역할 주입
- `lib/prisma.ts`, `lib/session.ts`(`getUserContext`), `middleware.ts`(로그인 게이트), `app/api/auth/[...nextauth]`, `types/next-auth.d.ts`
- `/api/search`·`/api/ask` → 실제 세션 부서/역할로 권한 검색 (`MOCK_USER` 제거)
- `app/(app)/` 라우트 그룹(Shell 적용) ↔ `app/login/` 풀스크린 로그인(Shell 없음, 디자인 적용)

### 검증 스크립트 (`scripts/`)
- `retrieval-check.mjs` — 관련성 + 권한 필터 회귀 (✅ 통과)
- `ask-check.mjs` — RAG→LLM 근거 답변 + 정직한 실패 (✅ 통과)

---

## 검증 상태

- ✅ `tsc --noEmit` 클린
- ✅ 검색: "출산휴가" top-1 = 휴가 규정 / People Ops는 재무 내부 문서 차단 / ADMIN은 접근
- ✅ 생성: 근거 [n] 인용 답변 / 코퍼스 밖 질문 → "찾지 못했어요"(환각 없음)
- ✅ 인용 클릭→출처 하이라이트 · 신뢰도 검색점수 산출 (코드/타입; UI 동작은 브라우저 확인)
- ⚠️ 브라우저 실렌더(스트리밍 UI·인용 클릭)는 사용자 직접 확인 (dev 서버는 로컬에서 실행)

---

## 다음 할 일

**다듬기 (등록 전):**
- Header 사용자 표시 실제 세션화(현재 정적 "준 김") + 로그아웃
- 메시지별 신뢰도 뱃지(현재 AI 헤더 뱃지 정적)
- 지식 소스 화면 mock → 실제 `content/` 반영
- UI: 한글 본문 폰트, 로그인/물어보기 미세조정

**그 다음 (A안 — axhub 등록·배포):**
- `axhub apps create` + `axhub.yaml` → 배포, 회사 Google 자격증명 주입으로 로그인 동작
- (필요시) `axhub apps update --field auth_mode=required`

**품질/후속:**
- bigram 검색 → 임베딩 + pgvector(설치 필요)
- 더 큰 LLM으로 스왑(7B 오타 개선)
- 대화/스레드 영속화, 피드백(👍/👎)·재생성

---

## 핵심 가드레일 (항상 유지)

- **환각 금지** — 검색 근거 안에서만 답, 근거 없으면 "모른다" (구현·검증됨)
- **권한 우선 검색** — 생성 전 검색 단계에서 범위 밖 소스 제외 (구현·검증됨)
- **API 키/모델 server-side 전용** — 클라이언트/로그 노출 금지
- **사내 데이터 외부망 미경유** — 로컬 Ollama로 충족 (클라우드 스왑 시 재검토)

---

## 환경 메모

- git repo 연결됨. 이 파일은 커밋마다 덮어쓴다.
- 로컬 Postgres 구동 중(`:5432`), `modle` DB 생성·마이그레이션·시드 완료. **pgvector는 미설치** — 임베딩 전환 시 설치 필요.
- `.env`/`.env.local`은 gitignore 대상(커밋 안 됨). 시크릿: `DATABASE_URL`, `AUTH_SECRET`(생성됨), `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`(비어있음 — axhub/회사 제공 예정), `GEMINI_API_KEY`, `NOTION_TOKEN`/`NOTION_ROOT_PAGE_ID`.
- 앱 실행 전: `ollama serve`(LLM) + Postgres 구동 필요. env 바꾸면 dev 재시작.
- 홈 디렉토리 떠돌이 lockfile(psql)로 인한 `npm run dev` freeze는 해결됨 (`next.config.ts`의 `turbopack.root`+`outputFileTracingRoot` + 홈 파일 정리).
- 레퍼런스: `/Users/kokooa/Work/assistant`(디자인), `/Users/kokooa/Work/Push`(권한/조직 모델 + axhub 인증 규약).
