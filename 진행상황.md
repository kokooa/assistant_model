# modle 진행 상황

> 사내 지식 AI 어시스턴트 — 직원이 자연어로 물어보면 사내 문서 안에서 근거(출처)와 함께 답하는 권한 인식형 챗봇.
> 전체 계획은 [`계획서.md`](./계획서.md)가 authoritative. 이 파일은 **현재 스냅샷**이며 커밋마다 덮어쓴다.

최종 갱신: 2026-06-02

---

## 한눈에 보기

| Phase | 내용 | 상태 |
|---|---|---|
| **Phase 1** | 디자인 프로토타입 (mock 데이터로 모든 화면) | ✅ 완료 |
| **Phase 2** | 지식 레이어 (검색/RAG, 권한 필터) | ✅ 완료 (bigram → 임베딩+pgvector 전환 완료) |
| **Phase 3** | 실제 LLM 연동 (스트리밍·인용·근거강제·정직한 실패) | ✅ 완료 (Gemini API) |
| **Phase 4** | 제품화 (인증·관리 UI·Notion sync·배포) | ✅ 완료 — axhub 점검 회복 대기 중 |
| **Phase 5** | Multi-tenant + Notion OAuth (여러 회사 제공) | 📋 설계 — axhub SSO claim 스펙 확보 후 착수 |

**지금 위치**: Phase 4 완료, axhub 배포만 남음. axhub hub-api 점검(`hub-api.jocodingax.ai` connection reset) 회복되면 즉시 배포 가능. 그 사이 Phase 5(여러 회사 시나리오) 설계.

---

## 기술 스택 (현재)

- Next.js 16 (App Router · React 19 · TS strict) · Tailwind 3 (`preflight: false`)
- **LLM**: Gemini API (`gemini-2.0-flash`), fallback Ollama. `LLM_PROVIDER=gemini` 기본
- **Embedding**: Gemini (`gemini-embedding-001`, 768차원), fallback Ollama bge-m3
- **DB**: Postgres + pgvector(설치·운영) + Prisma 6 — Department/User/Role + chunks 벡터 색인
- **인증**: next-auth v5(Auth.js) + Google(axhub 규약) + dev-only Credentials provider
- **빌드**: dev=Turbopack / production=Webpack (Turbopack module resolution 회피)

> ⚠️ **Turbopack ↔ middleware 비호환**: Next.js 16 + Turbopack 에서 middleware (Edge) 가 dev CPU 폭주 유발. 우회로 `app/(app)/layout.tsx` 에서 `auth()` + `redirect("/login")` 으로 (app) 그룹 보호 (next-auth v5 보조 패턴).

---

## 구현된 것

### 화면 (`app/`)
- 작업창 (`/`) — 사이드바·세션 목록·메시지·composer (workspace.css)
- 로그인 (`/login`) — 시드 계정 카드 + 회사 Google 로그인 (login.css)
- 설정 (`/settings`) — 계정 정보·로그아웃·관리 진입(ADMIN만)
- 관리자 (`/settings/admin`) — KPI / 부서 CRUD / Notion 동기화 / 유저 명시저장

### 컴포넌트 (`app/_components/`)
- Shell · Sidebar · Header · CmdPalette(⌘K) · HomeAsk(작업창 본체)

### 지식 레이어 / API
- `lib/embed.ts` — Gemini/Ollama 임베딩
- `lib/retrieve.ts` — pgvector cosine similarity + 권한 필터(GLOBAL / DEPT / ADMIN)
- `lib/rag.ts` — bigram 폴백 (pgvector 색인 0 일 때)
- `lib/llm.ts` — Gemini 스트리밍 + 근거강제 프롬프트
- `app/api/search/route.ts` — 권한 필터 검색(JSON)
- `app/api/ask/route.ts` — 검색→근거주입→NDJSON 스트림

### Notion 동기화
- `scripts/sync-notion.mjs` — Notion API → 청크 → 임베딩 → pgvector
- `app/_actions/admin.ts` — `syncNotionAction` 백그라운드 spawn
- 관리자 UI: Scope(GLOBAL/DEPARTMENT) + Root Page ID override + 마지막 동기화 표시

### 인증 / 권한 (Phase 4)
- `auth.config.ts` (edge-safe split) + `auth.ts` (Node, prisma 의존 callback)
- `app/(app)/layout.tsx` — session 체크 + redirect("/login")
- `lib/auth.ts` `ensureUser` — 회사 도메인 첫 로그인 시 자동 upsert, ADMIN_EMAILS 부트스트랩
- 명시 저장 패턴: 유저 카드 dirty rows + 일괄 저장 (`batchUpdateUsersAction`)
- 부서 CRUD + Notion sync 트리거 + 마지막 동기화 모니터링

### 검증 스크립트 (`scripts/`)
- `retrieval-check.mjs` · `ask-check.mjs` · `admin-check.mjs` · `bootstrap-check.mjs` · `scope-check.mjs` · `embed-check.mjs`

---

## 검증 상태

- ✅ `tsc --noEmit` 클린
- ✅ production build (`next build --webpack`) 성공
- ✅ 권한 필터: GLOBAL은 전체 / DEPARTMENT는 같은 부서만 / ADMIN은 부서 무관
- ✅ ADMIN_EMAILS 부트스트랩 idempotent (기존 user 보존)
- ✅ 인증 가드: 비로그인 `/` → `/login` redirect (layout 단)
- ✅ Notion 동기화 fire-and-forget spawn 동작
- ⏳ axhub 배포 검증 — 점검 회복 후

---

## 다음 할 일

**axhub 점검 회복 직후 (즉시):**
1. `axhub auth login` → 정상 로그인 확인
2. `axhub apps create modle` + env 7개 등록
   (`DATABASE_URL`, `AUTH_SECRET`, `GEMINI_API_KEY`, `NOTION_TOKEN`, `NOTION_ROOT_PAGE_ID`, `ADMIN_EMAILS`, `NODE_ENV=production`)
3. `axhub deploy` → 회사 Google 자격증명 자동 주입 확인
4. 회사 Google 로그인 → ensureUser + ADMIN 부트스트랩 검증
5. `/settings/admin` → 부서 셋업 + Notion sync 실행 + 다른 직원 권한 차이 확인

**Phase 5 — 여러 회사 시나리오 (axhub SSO claim 스펙 받은 후 착수):**
- 5.1 Multi-tenant 토대 (Organization 모델 + User/Department/Chunk에 orgId)
- 5.2 NotionIntegration 테이블 (회사별 토큰/루트페이지 DB 저장)
- 5.3 Notion OAuth flow + 페이지 picker UI
- 5.4 axhub SSO claim → Organization 자동 매핑

**품질/후속:**
- 대화/스레드 영속화 (현재 localStorage)
- 피드백(👍/👎) · 재생성
- 더 큰 LLM 모델 / 다른 provider 스왑

---

## 핵심 가드레일 (항상 유지)

- **환각 금지** — 검색 근거 안에서만 답, 근거 없으면 "모른다"
- **권한 우선 검색** — 생성 전 검색 단계에서 범위 밖 소스 제외
- **API 키 server-side 전용** — 클라이언트/로그 노출 금지
- **사내 데이터 외부망 미경유** — axhub 사내 endpoint 우선 (Gemini API 사용 시 회사 정책 확인)

---

## 환경 메모

- main 푸시 완료 (origin/main)
- `.env`/`.env.local` 은 gitignore. 시크릿: `DATABASE_URL`, `AUTH_SECRET`, `GEMINI_API_KEY`, `NOTION_TOKEN`, `NOTION_ROOT_PAGE_ID`, `ADMIN_EMAILS`, `GOOGLE_CLIENT_ID`/`SECRET`(배포 시 axhub 주입)
- 로컬 Postgres + pgvector 운영 중
- 디자인 자료(`handoff/`, `handoff 2/`, `handoff 3/`, 캡처 jpeg) 는 `.gitignore` 제외
- 레퍼런스: `/Users/kokooa/Work/assistant`(디자인), `/Users/kokooa/Work/Push`(권한/조직 모델)
