# modle — 사내 지식 AI 어시스턴트 계획서

> 한 줄 요약: **직원이 "출산휴가 며칠이야?", "재택근무 규정 알려줘" 처럼 자연어로 물어보면, 사내 정보 안에서 근거(출처)와 함께 답해주는 권한 인식형 대화형 AI 어시스턴트.**
> `/Users/kokooa/Work/assistant` 프로토타입의 디자인·UX 감성을 그대로 살리고, `/Users/kokooa/Work/Push`의 조직/권한 모델과 사내 콘텐츠를 지식 소스로 재활용한다.

작성일: 2026-05-28 · 위치: `/Users/kokooa/Work/modle` · 코드네임: **modle**

## 확정 사항 (2026-05-28)

1. **시작점**: Next.js 16 스캐폴드부터 (Push 검증 설정 재사용). Phase 1은 mock 데이터로 화면 먼저.
2. **이름**: `modle` 유지.
3. **1차 지식 코퍼스**: 휴가/근무 규정 1~2개로 작게 시작.
4. **LLM 제공자**: axhub LLM 게이트웨이.

> Phase 1은 백엔드(Prisma/auth) 없이 Next.js + 포팅한 assistant 디자인 + mock 데이터로 진행. Tailwind는 `preflight: false`로 넣어 포팅 CSS 디자인을 보존하면서 유틸리티만 사용.

## 변경 사항 (2026-06-02)

1. **LLM 제공자 변경**: axhub LLM 게이트웨이 없음 확인 → **Gemini API** (`gemini-2.0-flash`). fallback Ollama.
2. **임베딩 제공자**: Gemini `gemini-embedding-001` (768차원) + pgvector 색인.
3. **지식 소스 1차**: 사내 Notion 페이지 (NOTION_ROOT_PAGE_ID 의 자식 페이지 재귀 크롤).
4. **인증 가드 우회**: Next.js 16 + Turbopack ↔ middleware 비호환 → `app/(app)/layout.tsx` 단 보호.
5. **운영 시나리오 확장**: **여러 회사 제공** (SaaS 스타일) 으로 확장 결정 → Phase 5 추가.
6. **axhub 역할**: SSO(회사 단위 로그인) + Google 자격증명 주입 + 배포 게이트웨이. LLM/Notion 연동은 modle 자체 처리.

---

## 1. 무엇을 만드나 (제품 정의)

**핵심 가치**: 사내 정보가 인트라넷·메일·핸드북·노션·공지에 흩어져 있어서, 직원이 "이거 우리 회사는 어떻게 돼 있지?"를 알려면 사람한테 묻거나 문서를 헤맨다. modle은 그 질문을 **한 곳에서 자연어로 받아, 사내 데이터 안에서만, 근거와 함께** 답한다.

- **물어보기 중심** — 채팅이 제품의 허브. 공지·승인·액션센터 같은 운영 기능은 중심이 아니다.
- **근거 기반(grounded)** — 회사 문서/정책에서 찾은 내용으로만 답하고, 출처를 같이 보여준다. 근거가 없으면 "모른다"고 솔직하게 말한다 (환각 금지).
- **권한 인식(permission-aware)** — 사용자의 부서·역할 범위 안의 정보만 검색·인용한다. (Push의 `ADMIN / DEPT_ADMIN / MEMBER` + 부서 트리 재사용)
- **신뢰도 표시** — 답변마다 출처 수·관련도·신뢰도(High/Med/Low)를 보여줘서 직원이 믿을지 판단할 수 있게 한다.

**아닌 것**: 공지 발행 운영 대시보드(=Push)가 아니다. Push의 공지/통계/webhook 기능은 "지식 소스" 또는 부가 기능으로만 들어온다.

### 대표 사용자 시나리오

| 질문 예시 | 기대 동작 |
|---|---|
| "연차 며칠 남았고, 출산휴가는 며칠이야?" | HR 정책 문서에서 출산휴가 규정 인용 + (연동 시) 개인 잔여 연차 |
| "재택근무 신청 어떻게 해?" | 근무 규정/핸드북에서 절차 단계별 정리 + 출처 링크 |
| "Bay 4 화학물질 누출 SOP 알려줘" | 안전 매뉴얼에서 해당 SOP, 권한 있는 사람에게만 |
| "이번 주 우리 부서 공지 요약해줘" | 부서 범위 공지(Push 데이터)를 요약 |
| "경비 처리 한도 지역별로 알려줘" | 정책 문서 인용, 표로 정리 |

---

## 2. 두 레퍼런스에서 무엇을 가져오나

### `assistant`에서 (디자인·UX — 거의 그대로 계승)

- **디자인 토큰**: `styles.css`의 `:root` (oklch 따뜻한 페이퍼 배경 + 인디고 액센트, IBM Plex Sans/Mono, radius/shadow 스케일).
- **셸**: `Sidebar`(브랜드 + 그룹 네비 + 조직 푸터 + 보안 스트립), `Header`(AI 검색바 + 온라인 상태 + 아바타), **`CmdPalette` (⌘K)** — "Ask AI or jump to anything".
- **어시스턴트 화면 (제품의 중심)**: 3-pane 레이아웃 — 좌(대화 히스토리) · 중앙(채팅 + 컴포저: 모델 칩/첨부/소스 멘션/음성/글자수) · 우(**출처 + 권한 범위 + 신뢰도 + 관련 항목** 패널). 이 우측 패널이 modle의 차별점.
- **홈 대시보드**: 인사 히어로 + AI 요약 + **추천 프롬프트 칩** + 최근/오늘. modle에서는 "자주 묻는 질문/최근 대화" 중심으로 경량화.
- **아이콘 세트**(`icons.jsx`), **Tweaks 패널**(개발 중 디자인 토글 — dev 전용).

### `Push`에서 (자산·인프라 — 선택적 재활용)

- **권한/조직 모델**: `Role`(ADMIN/DEPT_ADMIN/MEMBER) + `Department` 트리 → 권한 인식 검색의 기반.
- **사내 콘텐츠**: `Notice`(공지) 데이터를 지식 소스 중 하나로 색인.
- **인프라 패턴**: Next.js 16(App Router·RSC·Server Actions) + Prisma/**Postgres** + next-auth + `lib/axhub.ts`(사내 인사/조직 데이터 Hub API) + axhub 한 줄 배포 + (선택) web-push.
- **AIBriefing / Action Center 패턴**: 홈의 "오늘의 브리핑"으로 축소 계승(부가 기능).
- **보안 원칙**: API 키는 server-side 전용, 사내 정보는 외부망 안 거치게(axhub 사내 endpoint).

---

## 3. 정보 구조(IA) / 화면

```
modle
├─ 물어보기 (Ask)        ← 제품의 중심. 3-pane 대화 화면
│   ├─ 대화 히스토리 (좌)
│   ├─ 채팅 + 컴포저 (중앙)
│   └─ 출처·권한·신뢰도 (우)
├─ 홈 (Home)            ← 인사 + 자주 묻는 질문 + 최근 대화 + 오늘의 브리핑(경량)
├─ 지식 소스 (Sources)   ← 어시스턴트가 아는 것: 정책/핸드북/공지/문서 + 색인 상태·소유자
├─ 공지 피드 (선택)       ← Push 자산. 지식 소스 겸 열람용
└─ 설정/관리 (Admin)     ← 지식 소스 등록·권한 범위·사용자/부서
```

⌘K 커맨드 팔레트는 어디서나 "물어보기"로 진입하는 1차 동선.

---

## 4. 시스템 아키텍처 (실제 LLM + 검색)

```
사용자 질문
   │
   ▼
[권한 컨텍스트]  ← 사용자 부서/역할 (next-auth 세션 + Push 모델)
   │
   ▼
[검색 / Retrieval]  질문 임베딩 → 권한 필터된 청크 top-k
   │  (Postgres + pgvector: 정책·핸드북·공지 청크 + 임베딩)
   ▼
[프롬프트 구성]  시스템 프롬프트 + 검색된 근거 청크(출처 메타 포함)
   │
   ▼
[LLM 생성]  Claude (claude-opus-4-7 / sonnet) · 스트리밍
   │  - 근거 안에서만 답
   │  - 근거 없으면 "모른다"
   │  - 인용 번호 → 출처 매핑
   ▼
[응답 + 출처 + 신뢰도]  우측 패널에 출처/관련도/권한 범위 렌더
```

- **지식 소스**: HR/휴가/복지/근무 규정, 사내 핸드북, 안전 SOP, 공지(Push) 등. 형식은 markdown/PDF/Notion/DB.
- **수집(Ingestion)**: 문서 → 청크 분할 → 임베딩 → 벡터 저장. (Push가 이미 Postgres라 **pgvector** 가 자연스러움)
- **검색**: 질문 임베딩 → 권한(부서/역할)으로 사전 필터 → top-k 청크.
- **생성**: 검색 청크를 컨텍스트로 주입, 인용 강제, 스트리밍 응답.
- **권한 인식**: 검색 단계에서 사용자 범위 밖 소스는 아예 제외 → 권한 없는 정보 누출 차단.
- **가드레일**: 근거 부족 시 솔직하게 모른다고 답(honest failure), 정책 환각 금지, PII 외부 전송 금지.

---

## 5. 기술 스택 & 시작점 결정

선택: **디자인 프로토타입 우선 → 실제 LLM 연동** + 위치 `/Users/kokooa/Work/modle`.

이 둘을 모두 만족시키는 방법에 **결정 포인트**가 하나 있다 (Phase 1 시작 전 확정 필요):

| 옵션 | 장점 | 단점 |
|---|---|---|
| **A. Next.js 16 스캐폴드부터** (Push 설정 재사용, 화면은 mock 데이터로 먼저) — *추천* | 프로토타입 코드가 그대로 실제 제품으로 이어짐. LLM 서버 라우트를 안전하게 붙일 수 있음(키 노출 X) | 초기 셋업 비용 약간 |
| B. assistant식 no-build React(HTML+Babel) 프로토타입 먼저 | 디자인 반복이 가장 빠름 | LLM 실연동 시 어차피 재작성, 정적 페이지는 API 키 안전하게 못 씀 |

> **추천**: A. modle에 Next.js 16 + Tailwind 3 + Prisma 스캐폴드를 만들되(= Push 검증된 설정 복사), Phase 1에서는 assistant의 컴포넌트/디자인 토큰을 옮겨와 **mock 데이터로 화면부터** 완성한다. "디자인 우선"의 취지(백엔드 전에 UX 확정)는 지키면서, 프로토타입이 버려지지 않는다.

확정 스택(추천안 기준): Next.js 16 · React 19 · TypeScript strict · Tailwind 3 · Prisma + Postgres(+pgvector) · next-auth · Claude API(또는 axhub LLM) · axhub 배포.

---

## 6. 단계별 로드맵

### Phase 1 — 디자인 프로토타입 (화면·UX 확정) · mock 데이터
**목표**: assistant 감성으로 modle의 모든 핵심 화면이 mock으로 동작.
- modle 스캐폴드 + assistant 디자인 토큰/아이콘 이식.
- 셸(사이드바·헤더·⌘K) 구성, IA를 "물어보기 중심"으로 재배치.
- 어시스턴트 3-pane 화면: mock 답변 + mock 출처/권한/신뢰도 패널.
- 홈(인사 + 자주 묻는 질문 + 최근 대화), 지식 소스 목록(mock).
- **검증**: `localhost:3000`에서 질문 → mock 답변 + 출처 패널이 보이고, ⌘K로 어디서나 물어보기 진입.

### Phase 2 — 지식 레이어 (검색/RAG 기반)
**목표**: 진짜 사내 문서에서 관련 근거를 찾아온다 (LLM 답변 전 단계).
- 지식 소스 정의 & 수집 파이프라인(문서 → 청크 → 임베딩 → pgvector).
- 1차 코퍼스: 휴가/HR/근무 규정 + 핸드북 + Push 공지.
- 권한 필터 검색(부서/역할) + 관련도 점수.
- **검증**: "출산휴가" 질문 시 관련 문서 청크 top-k가 올바른 출처와 함께 반환.

### Phase 3 — 실제 LLM 연동
**목표**: 검색 근거로 Claude가 인용·신뢰도와 함께 답한다.
- 서버 라우트(키 server-side 전용) + 시스템 프롬프트(근거 내 답변·인용 강제·모르면 모른다).
- 스트리밍 응답, 인용 번호 ↔ 출처 매핑, 신뢰도 산출(검색 점수 기반).
- 권한 인식 최종 검증(범위 밖 정보 누출 0).
- **검증**: 실제 질문 5종에 대해 근거 기반 답변 + 정확한 출처, 근거 없을 때 "모른다" 응답.

### Phase 4 — 제품화 ✅
- next-auth v5 로그인 (Google + dev Credentials).
- (app) 그룹 layout 인증 가드 (middleware 우회).
- 지식 소스 관리 UI: 부서 CRUD, 유저 일괄 명시 저장, Notion 동기화 트리거.
- ADMIN_EMAILS 부트스트랩 (첫 로그인 시 ADMIN, 기존 user 보존).
- Notion 동기화 (재귀 크롤 → 청크 → Gemini 임베딩 → pgvector).
- axhub 배포 준비 완료 — hub-api 점검 회복 후 `axhub deploy`.

### Phase 5 — Multi-tenant + Notion OAuth (여러 회사 제공)
**목표**: 여러 회사가 각자 Notion workspace 를 연결해서 자기 회사 데이터만 검색.
- **5.1 Multi-tenant 토대**: `Organization` 테이블 + `User`/`Department`/`chunks` 에 `organizationId`. retrieve 필터에 orgId 추가 (데이터 누수 차단).
- **5.2 NotionIntegration 테이블**: 회사별 토큰/workspace/rootPageId 를 DB 저장 (현재 env 고정 제거). sync 스크립트 orgId 기반 리팩토링.
- **5.3 Notion OAuth flow**: Public integration 등록 → `/api/notion/connect` → `/callback` → 토큰 자동 DB 저장.
- **5.4 Root 페이지 picker**: Notion search API 로 workspace 페이지 검색 + 관리자 선택 UI.
- **5.5 axhub SSO claim 매핑**: axhub 가 넘기는 회사 정보(domain/org_id) → `Organization` 자동 매핑. 새 회사 첫 로그인 시 Organization 자동 생성.

> 선행 조건: axhub SSO claim 스펙 확보 (claim 구조 = organization_id? company_email? domain?). 그에 따라 `Organization` 키 설계가 갈린다.

---

## 7. 보안·권한·가드레일

- **API 키 server-side 전용** — 클라이언트 컴포넌트/응답/로그에 노출 금지 (Push `AGENTS.md` 원칙 그대로).
- **권한 우선 검색** — 생성 전에 검색에서 범위 밖 소스 제외 → 권한 없는 정보가 LLM 컨텍스트에 아예 안 들어감.
- **근거 강제** — 검색 근거 밖의 내용 생성 금지, 인용 없는 정책 단정 금지.
- **사내 데이터 보호** — 사내 정보는 외부망 미경유(axhub 사내 endpoint 우선).
- **솔직한 실패** — 모르면 "사내 문서에서 찾지 못했어요"로 명시.

---

## 8. 데이터 / 지식 소스 전략 (열린 결정)

- **1차 코퍼스 출처**: 어디서 가져오나? (사내 위키/노션/구글드라이브/PDF 모음/Push 공지)
- **임베딩 모델**: 어떤 embedding 쓸지 (Claude/외부/사내).
- **벡터 저장소**: pgvector(추천, Postgres 재사용) vs 별도.
- **갱신 주기**: 문서 변경 시 재색인 전략(수동 vs 스케줄).

---

## 9. 제안 디렉토리 구조 (Phase 1 기준, 추천안 A)

```
modle/
├─ app/
│  ├─ page.tsx                 # 홈
│  ├─ ask/page.tsx             # 물어보기(3-pane)
│  ├─ sources/page.tsx         # 지식 소스
│  ├─ api/ask/route.ts         # (Phase 3) LLM 서버 라우트
│  └─ _components/             # Sidebar, Header, CmdPalette, Chat, SourcePanel ...
├─ lib/
│  ├─ retrieval.ts             # (Phase 2) 검색/RAG
│  ├─ llm.ts                   # (Phase 3) Claude 호출
│  ├─ permissions.ts           # 부서/역할 범위
│  └─ axhub.ts                 # 사내 데이터 (Push 재사용)
├─ prisma/schema.prisma        # User/Department + KnowledgeDoc/Chunk(+embedding)
├─ styles/ (or globals.css)    # assistant 디자인 토큰 이식
└─ 계획서.md
```

---

## 10. 다음 결정 (Phase 1 착수 전) — 모두 확정·반영됨

1. ~~프로토타입 스택~~ — A 확정 (Next.js 16 + Tailwind 3 + Prisma).
2. ~~제품/브랜드 이름~~ — `modle` 유지.
3. ~~1차 지식 코퍼스 출처~~ — 사내 Notion 페이지 (NOTION_ROOT_PAGE_ID).
4. ~~LLM 제공자~~ — Gemini API (`gemini-2.0-flash`).

## 11. 다음 결정 (Phase 5 착수 전)

1. **axhub SSO claim 스펙 확보** (선행). claim 구조 = `organization_id` / `company_email` / `domain` 중 무엇?
2. **Organization 키 설계**: claim 의 어떤 필드를 PK 로 잡을지.
3. **Notion OAuth Public integration 등록 책임**: modle 운영자가 등록 → 모든 회사가 같은 client 사용 vs 회사별 자체 등록.
4. **데이터 격리 검증 전략**: 다른 회사 chunk 접근이 0건임을 확인하는 자동화 스크립트 추가.
